/*
* 9. Define 4 Variables A,B,C and D then store in them any numbers and calculate them with formula :
  X = (A * B) - (C * D).
  then print the Output.

*/

main(){
  num A = 10 , B = 2.3 , C = 4.5 , D = 5;
  num X = (A * B) - (C * D); // 0.5
  print(X);
}
